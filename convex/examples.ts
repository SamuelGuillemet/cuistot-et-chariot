/**
 * Example mutations and queries to demonstrate the schema usage
 * 
 * NOTE: This file is commented out until Convex generates the necessary types
 * by running `npx convex dev`. The schema and validators are ready to use.
 * 
 * To enable this file:
 * 1. Run `npx convex dev` to generate types
 * 2. Uncomment the code below
 * 3. The mutations and queries will be available for testing
 */

/*
import { mutation, query } from "./_generated/server";
import { userValidators, homeValidators, productValidators } from "./validators";
import { v } from "convex/values";

// All the example code would go here...
// (commented out to avoid compilation errors before Convex setup)
*/

export {}; // Empty export to make this a valid module