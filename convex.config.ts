import { defineApp } from "convex/server";

/**
 * Convex configuration for Recipe Management Application
 */
const app = defineApp();

// Add authentication and other components here when needed
// app.use(authComponent);

export default app;